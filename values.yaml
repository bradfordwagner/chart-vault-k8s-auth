# Default values for vault.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicas: 1

image:
  repository: quay.io/bradfordwagner/vault-k8s-auth
  tag: 1.0.0-alpine_3.13

resources:
  limits:
    cpu: 50m
    memory: 50Mi

service_account: vault-auth
